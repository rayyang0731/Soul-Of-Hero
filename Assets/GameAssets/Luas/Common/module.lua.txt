---module 模块
---@param modName string 模块名称
---@return table
function module(modName)
    local tab = {}
    _G[modName] = tab
    print(modName)
    package.loaded[modName] = tab
    setmetatable(tab , { __index = _G })
    _ENV[modName] = tab
    return tab
end
